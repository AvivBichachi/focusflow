export function createTask(req, res) {
  res.status(501).json({
    message: "Create task - not implemented yet",
  });
}

export function listTasks(req, res) {
  res.status(501).json({
    message: "List tasks - not implemented yet",
  });
}
